import { FC } from "react";

const NewReview:FC = () => {
  
  return (
    <div style={{marginTop:"20px", marginBottom:"20px"}}>
          <span>Будет добавление отзыва нужен бэк</span>
    </div>
  );
};

export default NewReview;